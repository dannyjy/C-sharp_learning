using Arrays.leve1;
using Arrays.leve2;

namespace Arrays
{
    public class Program{
        static void Main(string[] args)
        {
            // NormalArrays n = new NormalArrays();
            // top10 top = new top10();

            // top10.First5();
            // top10.Last5();
            // n.stringArray(3);
            // n.intergerArray(5);
            // NormalArrays.General();
            // Level2.First5();
            // Level3.AllTop10();
            // int occured = Level3.Occurrences([2,5,7,9,2,34,9,34,2],1);
            // Console.WriteLine(occured);

            List<int> coordinates = [1, 2, 3, 4, 5];
            // Level3.SwapLastAndFirst(coordinates).ForEach((ele) => Console.WriteLine(ele));
            
            // M2List.M2Top10();
        }
    }
}